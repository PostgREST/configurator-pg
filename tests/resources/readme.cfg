# listen address
hostname = "localhost"
port = 8000

#logdir = "$(HOME)/logs"
logdir = "/var/log"
logfile = "$(logdir)/log.txt"
loglevels = [1, 4, 5]

users {
  alice = "alice@example.com"
  bob   = "bob@example.com"
}

passwords {
  import "secrets/passwords.txt"
}
